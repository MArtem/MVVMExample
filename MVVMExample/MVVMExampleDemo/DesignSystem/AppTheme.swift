import SwiftUI

enum AppTheme {
    static let backgroundBase = Color(.systemGroupedBackground)
    static let surfacePrimary = Color(.secondarySystemGroupedBackground)
    static let surfaceSecondary = Color(.systemBackground)
    static let textPrimary = Color.primary
    static let textSecondary = Color.secondary
    static let textTertiary = Color(.tertiaryLabel)
    static let actionPrimary = Color.accentColor
    static let divider = Color(.separator)
    static let destructive = Color.red
}
